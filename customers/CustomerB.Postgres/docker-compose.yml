services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: customer_b
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - customer-b-postgres:/var/lib/postgresql

volumes:
  customer-b-postgres:
